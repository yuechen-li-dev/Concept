module Main;

enum PairResult {
    Ok(int left, int right),
    Err(int code),
};

int main() {
    PairResult pair = PairResult::Ok(3, 4);
    match (pair) {
        PairResult::Ok(left, right) => return left + right;
        PairResult::Err(code) => return code;
    }
}
