module Main;

comptime int sumTo(int n) {
    int i = 0;
    int total = 0;

    while (i <= n) {
        total = total + i;
        i = i + 1;
    }

    return total;
}

static_assert(sumTo(9) == 45);

int main() {
    return comptime sumTo(9);
}
