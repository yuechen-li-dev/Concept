# R7g1 convergence log

| Blocker | Minimal evidence | Root cause and general fix | Effect |
| --- | --- | --- | --- |
| Comptime returned values only | `derive DeriveValue reflect<Item>` could not enter `Module.Functions` | Added structural generator and derivation declarations and inserted ordinary `FunctionDecl` before final analysis | Generated functions reach normal binding, checking, MIR, and C |
| Reflection probe corrupted later generic checking | Standard derived collector failed `CV4102` on `CollectorHandle<DerivedNode>` | Exact-callable prepass rewrote shared declaration slices during the signature probe; clone the mutable declaration slices for that probe | Standard generated Trace fixture passes |
| Generated output was invisible to consumers | A derived `Sum` in B was absent from C when C loaded B's artifact | Store the materialized function in B's portable semantic payload and exports; leave B's derivation request local | Artifact-only C compiles without rerunning B's generator |
| Generated origin was not inspectable | No generated symbol command or origin proof | Store a semantic generated identity and structured provenance on `FunctionDecl`; add structured inspection and generated proof nodes | Artifact and source inspection retain generator, site, type, and field inputs |
| Collisions had a one-sided ordinary error | A second derivation or handwritten `ReadValue` conflicted | Diagnose duplicate signatures before insertion with both sites | Stable conflict diagnostics |
| Generator recursion lacked a bounded boundary | A `derive` inside a generator body could be parsed as an unrelated expression error | Reject generation requests inside generator bodies with `GENERATOR_RECURSION_UNSUPPORTED`; keep one generation pass | No recursive expansion cycle |
| Standard imported generic types failed when the probe shared mutable slices | Artifact-only A/B/C `Trace<Node>` derivation needed an imported `[[reflect]]` node and generic `CollectorHandle<Node>` | Clone declaration slices before the reflection probe and preserve generated functions in B's artifact | A artifact only → B derivation → C artifact-only generated consumption passes |
| Exact callable return inference needed its source body | A module containing `auto MakeCallback()` and a derive request failed when the probe removed all bodies | Preserve inferred-return bodies in the reflection probe, while ordinary fixed-signature bodies remain deferred until generated names exist | Callable inference and generated declarations coexist |

The continuing boundary is metadata-driven construction of arbitrary declaration and control-flow shapes. `TestGeneratedPayloadEnumCaseExpansionBoundary` preserves a minimal enum generator using `foreach (EnumCaseInfo option in Cases<T>())`; it currently receives `GENERATOR_FIELD_QUERY_INVALID`. The current field query handles annotated struct fields. Payload-enum match generation, nested composite delegation, array/table traversal, and general concept-based field classification have not been implemented. The next expansion must remain structural and use ordinary semantic checking. Native Standard facts compare generated and handwritten tracing on the same cycles-and-diamonds graph and prove the generated witness `NoAllocation` through ordinary analysis.
