profile Core;

interface Reader<T>
{
    requires async int Read(ref const T self);
}

struct Device
{
    int value;

    async int Read(ref Device self)
    {
        return 1;
    }
}

requires Reader<Device>;
