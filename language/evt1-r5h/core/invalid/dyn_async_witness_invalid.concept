profile Core;

interface Reader<T>
{
    requires async int Read(ref T self);
}

struct Device
{
    int value;
}

int Main()
{
    Device device = Device{1};
    dyn Reader reader = ref device;
    return 0;
}
