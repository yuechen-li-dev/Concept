# name: maybe moved argument after if
# phase: backend-c
# expect: fail

=== source ===
module Main;

struct Box {
    int value;
};

int take(Box b) {
    return b.value;
}

int main() {
    bool cond = false;
    Box a = Box { value: 42 };

    if (cond) {
        Box b = move a;
    }

    return take(move a);
}

=== diagnostics ===
CON0155
