# name: implicit copy param forward
# phase: backend-c
# expect: fail

=== source ===
module Main;

struct Box {
    int value;
};

int take(Box b) {
    return b.value;
}

int forward(Box b) {
    return take(b);
}

int main() {
    Box a = Box { value: 42 };
    return forward(move a);
}

=== diagnostics ===
CON0154
