profile Core;

ReadOnlySpan<int> Forward(ReadOnlySpan<int> source)
{
    return source;
}

concept SameBacking<T>
{
    requires T Forward(T source);
    requires compiler.SameRegion(source, result);
}

requires SameBacking<ReadOnlySpan<int>>;

