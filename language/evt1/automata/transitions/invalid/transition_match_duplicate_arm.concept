profile Core;

enum Signal
{
    Start,
    Stop,
}

automata Bad
{
    machine Run
    {
        state Idle
        {
            transition match (Signal::Start)
            {
                Signal::Start => Idle;
                Signal::Start => Idle;
                Signal::Stop => Idle;
            }
        }
    }
}
