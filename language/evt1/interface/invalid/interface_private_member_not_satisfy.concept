profile Core;

interface Resettable<T>
{
    requires void Reset(ref T self);
}

class Device
{
public:
    int state;

private:
    void Reset()
    {
        state = 0;
    }
}

requires Resettable<Device>;
