profile Core;

interface Resettable<T>
{
    requires void Reset(ref T self);
}

struct Device
{
    int state;

    void Reset(ref Device self)
    {
        self.state = 0;
    }
}

void InvalidCall(ref Device device)
{
    dyn Resettable resettable = ref device;
    resettable.Start();
}
