profile Core;

ref int UnknownReference();

int UnknownLifetimeOrder()
{
    ref int source = UnknownReference();
    ref int destination = UnknownReference();
    Assert.Concept<Outlives>(
        source,
        destination,
        "source must outlive destination"
    );
    return 0;
}
