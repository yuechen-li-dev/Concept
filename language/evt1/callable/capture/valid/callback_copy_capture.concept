profile Core;

int Main()
{
    int multiplier = 3;
    const auto scale = callback(int value)
    with (multiplier)
    {
        return value * multiplier;
    };
    multiplier = 9;
    return scale(4);
}
