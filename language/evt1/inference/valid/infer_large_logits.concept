profile Core;
enum Action
{
    Attack,
    Idle
}
float Main()
{
    Inference<Action> belief = infer
    {
        Attack score 1000.0;
        Idle score 1001.0;
    };
    return belief[Action::Attack] + belief[Action::Idle];
}
