profile Core;

struct Resource
{
    int handle;
}

void Drop(owned Resource resource)
{
    return;
}

int Consume(owned Resource resource)
{
    return resource.handle;
}

int Main()
{
    owned Resource resource = Resource{42};
    return Consume(move resource);
}
