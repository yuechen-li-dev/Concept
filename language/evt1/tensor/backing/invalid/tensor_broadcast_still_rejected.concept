profile Core;

int TensorBroadcastStillRejected()
{
    tensor<int> matrix[2, 3] = [[1, 2, 3], [4, 5, 6]];
    tensor<int> row[3] = [10, 20, 30];
    tensor<int> result[2, 3] = 0;
    result = matrix + row;
    return 0;
}
