# name: dyn type hir surface
# phase: check
# check: hir
# expect: pass

=== source ===
module Interfaces;

interface Writer {
    void Write(int value);
};

interface Reader {
    int Read();
};

void Pipe(dyn Reader& reader, mut dyn Writer& writer);
