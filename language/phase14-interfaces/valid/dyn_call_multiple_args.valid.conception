# name: dyn call multiple args
# phase: check
# check: hir
# expect: pass

=== source ===
module Main;

interface Writer {
    int WritePair(int left, bool flag);
};

int Emit(mut dyn Writer& writer) {
    return writer.WritePair(7, true);
}
