# name: dyn multiple interfaces
# phase: check
# check: hir
# expect: pass

=== source ===
module Interfaces;

interface Reader {
    int Read();
};

interface Writer {
    void Write(int value);
};

void UseReader(dyn Reader& reader);
void UseWriter(mut dyn Writer& writer);
