# name: interface requirement struct param
# phase: check
# check: hir
# expect: pass

=== source ===
module Interfaces;

struct Buffer {
    int length;
};

interface Writer {
    void Write(Buffer buffer);
};
