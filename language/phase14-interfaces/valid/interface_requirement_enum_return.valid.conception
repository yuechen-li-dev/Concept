# name: interface requirement enum return
# phase: check
# check: hir
# expect: pass

=== source ===
module Interfaces;

enum WriteState {
    Ready,
    Blocked
};

interface Writer {
    WriteState State();
};
