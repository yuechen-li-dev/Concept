profile Core;

concept ContiguousTensor<T>
{
    requires compiler.Contiguous<T>();
    requires compiler.Bounded<T>();
    requires compiler.Rank<T>(2);
    requires compiler.NoOwnershipTransfer<T>();
}

requires ContiguousTensor<tensor<int, 2>>;
