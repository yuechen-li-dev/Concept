profile Core;

int RebindRuntimeView(int rows, int columns)
{
    int<array>[6] storage = [1, 2, 3, 4, 5, 6];
    ref int<ndarray>[rows, columns] matrix = bind storage;
    ref int<array>[6] flat = bind matrix;
    flat[4] = 12;
    return matrix[1, 1];
}
