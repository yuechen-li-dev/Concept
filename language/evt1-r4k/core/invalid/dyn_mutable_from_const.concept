profile Core;

interface Inspectable<T>
{
    requires int Inspect(ref const T self);
}

struct Item
{
    int value;

    int Inspect(ref const Item self)
    {
        return self.value;
    }
}

int InvalidMutability(ref const Item item)
{
    dyn Inspectable inspectable = ref const item;
    return inspectable.Inspect();
}
