# name: arena alloc generic non drop
# phase: check
# check: hir
# expect: pass

=== source ===
module Example;

template<T>
alloc T* Make(Arena* arena, T* seed) {
    return Arena.alloc<T>(arena);
}

alloc int* MakeInt(Arena* arena, int* seed) {
    return Make(arena, seed);
}

