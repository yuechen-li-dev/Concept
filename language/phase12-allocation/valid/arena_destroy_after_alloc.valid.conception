# name: arena destroy after alloc
# phase: check
# check: hir
# expect: pass

=== source ===
module Example;

alloc int DestroyAfterAlloc(Arena* arena) {
    int* value = Arena.alloc<int>(arena);
    Arena.destroy(arena);
    return 0;
}

