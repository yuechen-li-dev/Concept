# name: machine transition match bad pattern
# phase: parse
# expect: fail

=== source ===
module Example;

machine M(int code) -> int {
    state Start {
        transition match (code) {
            + => Start;
        };
    }
}

=== diagnostics ===
CON0001
