# name: panic backend helper emitted
# phase: backend-c
# expect: pass

=== source ===
module Main;

int main() {
    panic("intentional runtime failure");
    return 0;
}

=== c ===
contains: static void cpt_panic(const char* reason)
contains: fprintf(stderr, "panic: %s\n", reason);
contains: exit(101);
contains: cpt_panic("intentional runtime failure");
not_contains: Assert.
