# name: machine result before completion example exits 101
# phase: run
# expect: pass

=== source ===
module Main;

machine Simple() -> int {
    state Start {
        return 7;
    }
}

int main() {
    Simple m = Simple();
    return Result(m);
}

=== run ===
exit_code: 101
