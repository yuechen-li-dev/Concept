# name: assert statement condition expression
# phase: check
# check: hir
# expect: pass

=== source ===
module Main;

int main() {
    int x = 1;
    assert(x == 1, "x should remain initialized");
    return 0;
}
