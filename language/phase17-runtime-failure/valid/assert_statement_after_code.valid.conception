# name: assert statement after code
# phase: check
# check: hir
# expect: pass

=== source ===
module Main;

int main() {
    int x = 1;
    x = x + 1;
    assert(x == 2, "x should advance once");
    return x;
}
