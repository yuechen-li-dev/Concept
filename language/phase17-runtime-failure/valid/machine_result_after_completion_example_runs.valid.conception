# name: machine result after completion example runs
# phase: run
# expect: pass

=== source ===
module Main;

machine Simple() -> int {
    state Start {
        return 7;
    }
}

int main() {
    Simple m = Simple();
    step(m);
    return Result(m);
}

=== run ===
exit_code: 7
