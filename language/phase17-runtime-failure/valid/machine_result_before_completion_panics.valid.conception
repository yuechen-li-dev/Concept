# name: machine result before completion panics
# phase: run
# expect: pass

=== source ===
module Main;

machine Simple() -> int {
    state Start {
        return 7;
    }
}

int main() {
    Simple m = Simple();
    return Result(m);
}

=== run ===
exit_code: 101
