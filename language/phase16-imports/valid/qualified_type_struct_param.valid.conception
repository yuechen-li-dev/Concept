# name: qualified type struct param
# phase: check
# check: declarations
# expect: pass

=== file: Geometry.concept ===
module Geometry;

struct Point {
    int x;
    int y;
};

=== file: Main.concept ===
module Main;

import Geometry;

int Sum(Geometry.Point p) {
    return p.x + p.y;
}
