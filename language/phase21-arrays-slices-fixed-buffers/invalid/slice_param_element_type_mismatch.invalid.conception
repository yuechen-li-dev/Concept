# name: phase21 slice parameter element mismatch
# mode: check
# expect_error: CON0418

int first(Slice<bool> values) {
    if values[0] { return 1; }
    return 0;
}

int main() {
    int[3] values = [4, 5, 6];
    return first(values);
}
