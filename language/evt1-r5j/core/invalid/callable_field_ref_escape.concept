profile Core;

auto MakeReader(ref int value)
{
    return callback()
    with (ref value)
    {
        return value;
    };
}

using Reader = typeof(MakeReader);

struct EscapingHolder
{
    Reader read;
}

int Main()
{
    return 0;
}
