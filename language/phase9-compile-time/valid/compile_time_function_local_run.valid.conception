# name: compile-time function local run
# phase: run
# expect: pass

=== source ===
module Main;

comptime int twice(int x) {
    int y = x * 2;
    return y;
}

int main() {
    return comptime twice(21);
}

=== run ===
exit_code: 42
