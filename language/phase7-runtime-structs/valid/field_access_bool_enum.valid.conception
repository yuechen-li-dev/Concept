# name: phase7 field access bool enum
# phase: check
# check: hir
# expect: pass

=== source ===
module Main;

enum Mode { Fast; Slow; };
struct Header { bool ok; Mode mode; };

int main() {
    Header h = Header { mode: Mode::Fast, ok: true, };
    if (h.ok) { return 1; }
    return 0;
}
