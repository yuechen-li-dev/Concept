profile Core;

int<array>[3] MakeValues(ref int calls)
{
    calls = calls + 1;
    return [2, 3, 4];
}

int Main()
{
    int calls = 0;
    int total = 0;
    foreach (int value in MakeValues(ref calls))
    {
        total = total + value;
    }
    return calls * 10 + total;
}
