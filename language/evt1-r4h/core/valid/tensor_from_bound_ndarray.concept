profile Core;

int TensorFromBoundNDArray(int rows, int columns)
{
    int<array>[6] storage = [1, 2, 3, 4, 5, 6];
    ref int<ndarray>[rows, columns] matrix = bind storage;
    tensor<int, 2> values = Tensor(matrix);
    return values[1, 2];
}
