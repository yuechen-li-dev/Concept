# name: transition match duplicate true
# phase: check
# check: hir
# expect: fail

=== source ===
module Main;

machine M(bool flag) -> int {
    state Start {
        transition match (flag) {
            true => Done;
            true => Done;
            false => Done;
        };
    }
    state Done { return 0; }
}

=== diagnostics ===
CON0296
