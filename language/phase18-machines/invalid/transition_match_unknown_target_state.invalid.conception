# name: transition match unknown target state
# phase: check
# check: hir
# expect: fail

=== source ===
module Main;

machine M(bool flag) -> int {
    state Start {
        transition match (flag) {
            true => Missing;
            false => Done;
        };
    }
    state Done { return 0; }
}

=== diagnostics ===
CON0222
