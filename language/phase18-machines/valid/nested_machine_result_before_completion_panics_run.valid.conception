# name: nested machine result before completion panics run
# phase: run
# expect: pass

=== source ===
module Main;

machine Child { state Start { return 7; } }

machine Parent {
    Child child;
    state Start { return Result(child); }
}

int main() {
    Parent p = Parent();
    Step(p);
    return Result(p);
}

=== run ===
exit_code: 101
