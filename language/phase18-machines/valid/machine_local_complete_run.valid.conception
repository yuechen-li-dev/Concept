# name: machine local complete run
# phase: run
# expect: pass

=== source ===
module Main;

machine Simple() -> int {
    state Start {
        return 9;
    }
}

int main() {
    Simple m = Simple();
    if (Complete(m)) {
        return 10;
    }
    Step(m);
    if (Complete(m)) {
        return 1;
    }
    return 0;
}

=== run ===
exit_code: 1
