# name: phase5 enum match payload too many bindings
# phase: check
# expect: fail

=== source ===
module Main;

enum ParseResult { Ok(int value), Err(int code), };

int main() {
    ParseResult result = ParseResult::Ok(1);
    match (result) {
        ParseResult::Ok(value, extra) => return value;
        _ => return 0;
    }
}

=== diagnostics ===
CON0046
