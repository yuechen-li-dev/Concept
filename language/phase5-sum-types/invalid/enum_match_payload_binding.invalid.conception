# name: phase5 enum match payload binding unsupported
# phase: parse
# expect: fail

=== source ===
module Main;

enum ParseResult {
    Ok(int value),
    Err(int code),
};

int main() {
    ParseResult result = ParseResult::Ok(7);
    match (result) {
        ParseResult::Ok(value) => return 1;
        _ => return 0;
    }
}

=== diagnostics ===
CON0003
