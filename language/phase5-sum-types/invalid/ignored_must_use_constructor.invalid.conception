# name: phase5 ignored must use constructor
# phase: check
# check: hir
# expect: fail
# diagnostics: CON0049

=== source ===
module Main;

must_use enum ParseResult {
    Ok(int value),
    Err(int code),
};

int main() {
    ParseResult::Ok(1);
    return 0;
}
