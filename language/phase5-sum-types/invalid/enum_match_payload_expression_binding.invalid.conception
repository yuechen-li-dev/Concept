# name: phase5 enum match payload expression binding
# phase: parse
# expect: fail

=== source ===
module Main;

enum ParseResult { Ok(int value), Err(int code), };

int main() {
    ParseResult result = ParseResult::Ok(1);
    match (result) {
        ParseResult::Ok(1) => return 1;
        _ => return 0;
    }
}

=== diagnostics ===
CON0003
