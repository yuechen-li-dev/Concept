# name: phase5 try in int function
# phase: check
# check: hir
# expect: fail
# diagnostics: CON0051

=== source ===
module Main;

must_use enum ParseIntResult {
    Ok(int value),
    Err(int code),
};

ParseIntResult parseInt(int value) {
    return ParseIntResult::Ok(value);
}

int main() {
    int value = try parseInt(1);
    return value;
}
