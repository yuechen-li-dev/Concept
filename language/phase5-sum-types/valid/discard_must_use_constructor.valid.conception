# name: phase5 discard must use constructor
# phase: run
# expect: pass

=== source ===
module Main;

must_use enum ParseResult {
    Ok(int value),
    Err(int code),
};

int main() {
    discard ParseResult::Ok(1);
    return 0;
}

=== run ===
exit_code: 0
