# name: phase5 result shape not must use
# phase: check
# check: declarations
# expect: pass

=== source ===
module Main;

enum ParseIntResult {
    Ok(int value),
    Err(int code),
};

int main() {
    return 0;
}
