# name: phase3 unknown type closeout
# phase: check
# check: declarations
# expect: fail

=== source ===
module Example;

struct Box {
    Missing value;
};

enum MaybeBox {
    Some(Box value);
    Other(Missing fallback);
};

=== diagnostics ===
CON0021
CON0021
